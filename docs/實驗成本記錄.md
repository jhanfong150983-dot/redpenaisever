# 實驗成本記錄

> 每跑一次沙盒／實測就追加一列。費率權威＝`api/admin/[action].js` 的 `modelRates()`；
> 匯率 USD→TWD = 33；thoughts tokens 按**輸出價**計費（2026-07-17 起，漏算過就少算過半）。

| 時間 | 實驗名稱 | 模型 | 呼叫 | input tokens | output tokens | thoughts tokens | 總 tokens | 花費 (NT$) |
|---|---|---|---|---|---|---|---|---|
| 2026-08-30 23:57 | ⛔**作廢**・字形／注音三判官 v3 沙盒（2票+加賽） | gemini-3.5-flash | 141 | 184,638 | 7,927 | 0 | 192,565 | **11.49** |
| 2026-08-31 00:04 | 字形／注音三判官 v3 沙盒（2票+加賽）· 修正取圖後正式輪 | gemini-3.5-flash | 139 | 255,082 | 7,675 | 0 | 262,757 | **14.91** |
| 2026-08-31 02:00 | 注音仲裁者 C（v3 精簡版第三票） | gemini-3.5-flash | 39 | 93,495 | 2,402 | 0 | 95,897 | **5.34** |
| 2026-08-31 02:28 | 仲裁者 C 兩條新條款驗證（只跑 C） | gemini-3.5-flash | 32 | 86,159 | 2,788 | 0 | 88,947 | **5.09** |
| 2026-08-31 02:39 | 注音 crop ×3 放大對照（重測放大禁令） | gemini-3.5-flash | 52 | 123,812 | 2,940 | 0 | 126,752 | **7.00** |
| 2026-08-31 03:47 | 注音放大・因果＋機制驗證（5 輪 × 3 配置） | gemini-3.5-flash | 150 | 356,970 | 8,557 | 0 | 365,527 | **20.21** |
| 2026-09-02 23:17 | ⛔**作廢**・社會 fill_blank read 模型 A/B（2.5 臂全 400） | gemini-2.5-flash ＋ gemini-3.6-flash | 58 | 25,114 | 1,714 | 0 | 26,828 | **0.83** |
| 2026-09-02 23:20 | 社會 fill_blank read 模型 A/B（2.5 vs 3.6） | gemini-2.5-flash ＋ gemini-3.6-flash | 58 | 42,050 | 3,444 | 5,061 | 50,555 | **0.95** |
| 2026-09-03 00:08 | 社會 1-1-1 完整兩讀改 gemini-3.6-flash（read1 盲＋read2 知答） | gemini-3.6-flash | 58 | 51,301 | 3,458 | 0 | 54,759 | **1.70** |
| 2026-09-03 00:30 | 社會 4-5-5-1（compound_chain_table）read 升 3.6・B 臂 | gemini-3.6-flash ＋ gemini-2.5-flash | 87 | 146,562 | 16,639 | 52,689 | 215,890 | **2.70** |
| 2026-09-03 06:36 | 社會 4-5-5-1 rubric 判官・C 臂（不抄寫、直接看圖判分） | gemini-3.6-flash | 29 | 50,344 | 5,359 | 0 | 55,703 | **1.91** |
| 2026-09-03 17:41 | 社會卷 rubric 判官・production 裁圖（8 題 × 抽樣） | gemini-3.6-flash | 64 | 112,720 | 12,552 | 0 | 125,272 | **4.34** |
<!-- ROWS -->

## 各次明細
<!-- DETAILS -->

### 2026-09-03 17:41｜社會卷 rubric 判官・production 裁圖（8 題 × 抽樣）

- **開始／結束**：2026-09-03 17:41 → 2026-09-03 17:44（歷時 3.2 分）
- **與前一輪 armC 的差別**（user 指正）：① crop 改用 **production 的**（`cropWithMarkByBbox`，padY 0.048÷6＝read 收到的同一份 bytes），armC 自行把 padY 撐到 0.055 不是 production 會產生的東西；② 改抽樣
- **可上線性**：判官重用 Phase A 已有的 `allQuestionCropMap`，**零新增裁圖程式**；落點是 Phase B 取代「accessor 從文字判分」，read 照跑（檢討單／家長報告需要文字）
- **範圍**：這張卷 40 題中帶 `rubricsDimensions` 的 8 題（2-3-5、2-3-6、3-4-4、4-5-5-1、4-5-5-2、5-6-6、6-7-8-1、6-7-8-2）＝開放式概念題；其餘 32 題唯一答案不適用。⚠ 國語不適用（其 rubric criteria 100% 寫「用字書寫正確」＝錯字是評分對象，社會／自然 0%）
- **抽樣**：8 生，依 8 題 rubric 總得分分層（3／18／21／23／25／26／27／28 分）
- **prompt 三處修正**：維度由 `rubricsDimensions` 動態生成；**evidence 排在 score 之前**（對齊字形 blocks→verdict／注音 analysis→verdict 的慣例，armC 寫反了）；圈選類＝視覺判定、語句通順≠錯字、畫面不足→該維度 0 分並寫入 uncertain
- **呼叫**：64 次（失敗 0）
- **花費**：**NT$ 4.34**（每格 NT$ 0.068）
- **結果**：與現行同分 54／64，判官較高 9、較低 1；自陳沒把握 1 格
- ⚠ 無 ground truth，分數對錯要 user 人工判
- **產出**：`local-only/社會_rubric判官_production裁圖.html`、`local-only/_rubricjudge-raw.json`


### 2026-09-03 06:36｜社會 4-5-5-1 rubric 判官・C 臂（不抄寫、直接看圖判分）

- **開始／結束**：2026-09-03 06:36 → 2026-09-03 06:38（歷時 1.3 分）
- **緣起**：B 臂（read 升 3.6）長句擷取明顯改善（整段走樣消失、兩讀分歧 20→14），但**人名照樣錯且更危險**——2.5 拼怪字、3.6 用歷史知識補真實人名（`ojri17` → 「李春生」，真人但不在本卷清單）。5 份分數變動，2 份肇因於人名。
- **假設**：判官不必抄對人名，只需判斷「是不是清單裡的那一位」。**沒有轉寫步驟 → 人名幻覺無從發生**。
- **設計**（⚠ 新元件、無 production 可鏡像）：crop 用同一個 `cropWithMarkByBbox` 但 padY 0.008 → **0.055**（含人物清單與題幹，紅框仍標本列）；prompt 參照 `level-rubric-grader.js` 的 `buildElementQuestionPrompt`（逐項判定＋evidence＋uncertain），但一次問三個維度以省成本；明文禁止用自身歷史知識補人名；temp 0＋thinking MINIMAL（比照 VJ_GRADE）＋ MEDIA_RESOLUTION_HIGH（判官家族慣例）
- **呼叫**：29 次（失敗 0）
- **花費**：**NT$ 1.91**（每份 NT$ 0.066）
- **結果**：解析成功 29/29；C 與 B 同分 20／C 與現行同分 21
- ⚠ 分數對錯無現成 ground truth，要 user 人工判（三臂並排 HTML）
- **產出**：`local-only/社會4-5-5-1_三臂對照.html`、`local-only/_4551-armC-raw.json`


### 2026-09-03 00:30｜社會 4-5-5-1（compound_chain_table）read 升 3.6・B 臂

- **開始／結束**：2026-09-03 00:30 → 2026-09-03 00:37（歷時 7.0 分）
- **問題**：`compound_chain_table` 走 2.5，現行**兩讀分歧 20/29**（fill_blank 只有 4/29）。2.5 把「斯文豪」讀成「其實憂／其升文豪／其紋豪」，長句整段走樣。
- **B 臂設計**：**只換 read 模型**，accessor 不動（差異才能歸因到 read）。crop＝`cropWithMarkByBbox`(0.012/0.008)；read1 抄寫員、read2 校對員帶 referenceAnswer；family='compound' 的「｜分欄」規則；**accessor 直接呼叫 production 的 `buildAccessorPrompt()`**（exported，未重建），`compound_chain_table` 不在 `cropTypesForAccessor` → 純文字單一 part、2.5、temp 0
- **呼叫**：87 次（read 58／accessor 29；失敗 0）
- **花費**：read NT$ 1.82／accessor NT$ 0.88 ＝ **NT$ 2.70**（每份 NT$ 0.093）
- **結果**：兩讀分歧 20 → **14**；分數與現行不同 5/29 份
- ⚠ 分數對錯要 user 人工判（rubric 三維度、開放答案，無現成 ground truth）
- **產出**：`local-only/社會4-5-5-1_read升3.6.html`、`local-only/_4551-armB-raw.json`


### 2026-09-03 00:08｜社會 1-1-1 完整兩讀改 gemini-3.6-flash（read1 盲＋read2 知答）

- **開始／結束**：2026-09-03 00:08 → 2026-09-03 00:09（歷時 1.1 分）
- **目的**：user 要「完全照 production、只換模型」的兩讀沙盒，親自判讀值。前一輪只跑了盲讀單臂。
- **鏡像**：crop = `cropWithMarkByBbox`(padX 0.012／padY 0.008，紅框 set-of-mark)；read1 role='detail'（抄寫員、標籤 `--- 題目 1-1-1 ---`）；read2 role='review'（校對員、標籤 `--- 題目 1-1-1（正確答案：一府二鹿三艋舺） ---`）；兩讀並行；gen 依 model-adapter 逐模型剝參數
- **對照組**：資料庫現行的 readAnswer1／readAnswer2（2.5 跑出來的），未重跑
- **呼叫**：58 次（失敗 0）
- **token**：input 51,301、output 3,458、thoughts 0、合計 **54,759**
- **花費**：**NT$ 1.70**（每份 NT$ 0.059）
- **結果**：兩讀一致 新 29/29、舊(2.5) 25/29；疑似知答洩漏（read2 等於標答但 read1 不是）0 份
- ⚠ 題幹在 crop 內（fill_blank wide bbox 的設計），模型可由題幹推知答案——這條洩漏管道兩個模型都有，不是升級才出現
- **產出**：`local-only/社會1-1-1_兩讀改3.6.html`、`local-only/_social-tworead36-raw.json`


### 2026-09-02 23:20｜社會 fill_blank read 模型 A/B（2.5 vs 3.6）

- **開始／結束**：2026-09-02 23:20 → 2026-09-02 23:22（歷時 2.4 分）
- **問題**：社會（非國英數）的 `fill_blank`/`short_answer` 走 MODEL_FLASH=2.5，同卷 `single_choice` 卻走 MODEL_PRO=3.6。手寫「一府二鹿三艋舺」被拆成部件（艋＝舟+孟、舺＝舟+甲）。國語卷整卷升 3.6 的理由正是「手寫國字幻覺」，社會沒吃到。
- **既有依據不足**：查遍 docs/ 與 local-only/，**沒有任何實驗測過「非國語卷的中文手寫 fill_blank，2.5 vs 3.6」**。type-split 設計文件自訂的規矩是「每型上線前跑該型 A/B 再定 model 欄」，`fill_blank` 那格標的是「先 2.5-split 驗」後就停在 2.5。
- **設計**：同一張紅框 set-of-mark 裁圖（padX 0.012／padY 0.008）、同一份 `tsReadHead('text','detail')` prompt、同一組生成參數（temp 0／thinking MINIMAL／MEDIA_RESOLUTION_MEDIUM），**只換模型**。29 份 × 2 模型 × 1 輪
- **呼叫**：58 次（失敗 0）
- **費率**：2.5 = $0.075/$0.30；3.6 = $0.75/$3.75（促銷至 2026-12-31，之後翻倍）；匯率 33
- **花費**：2.5 NT$ 0.109／3.6 NT$ 0.837 ＝ **NT$ 0.95**（3.6 是 2.5 的 7.7 倍）
- **結果**：讀值與標準答案完全一致 — 本輪 2.5 17/29、本輪 3.6 25/29（production 當時 16/29）；只有 3.6 讀對 8 份、只有 2.5 讀對 0 份
- ⚠ 「完全一致」是代理指標（學生本來就可能寫錯），最終要人工判逐格圖
- **產出**：`local-only/沙盒_社會read模型AB.html`、`local-only/_social-readmodel-raw.json`


### 2026-09-02 23:17｜⛔ 作廢・社會 fill_blank read 模型 A/B（2.5 臂全 400）

> **⛔ 對照組不存在，結論不可採信。** 腳本對兩個模型送同一份 generationConfig，
> 但 production 的 `model-adapter` 會**逐模型剝除**不支援的參數：`thinking_level` 只有
> `/3\.[56]-flash/` 吃、`mediaResolution` 只有 `gemini-3.x` 吃（model-adapter.js:204-254）。
> 2.5-flash 兩個都收到 → 29 次全部 HTTP 400 → 「2.5 得 0/29」是 API 錯誤不是讀不出來。
> **修法**：腳本複製 adapter 的剝除規則，並在失敗時印出 HTTP 狀態與訊息（原本靜默 return null，
> 才會整臂全滅還沒被發現）。重跑見下一筆。
> 又一次踩到 [feedback_local_experiment_must_mirror_production_input]：鏡像不只是 prompt 和圖，
> **送出去的參數也要照 adapter 的規則逐模型調整**。

- **開始／結束**：2026-09-02 23:17 → 2026-09-02 23:18（歷時 1.3 分）
- **問題**：社會（非國英數）的 `fill_blank`/`short_answer` 走 MODEL_FLASH=2.5，同卷 `single_choice` 卻走 MODEL_PRO=3.6。手寫「一府二鹿三艋舺」被拆成部件（艋＝舟+孟、舺＝舟+甲）。國語卷整卷升 3.6 的理由正是「手寫國字幻覺」，社會沒吃到。
- **既有依據不足**：查遍 docs/ 與 local-only/，**沒有任何實驗測過「非國語卷的中文手寫 fill_blank，2.5 vs 3.6」**。type-split 設計文件自訂的規矩是「每型上線前跑該型 A/B 再定 model 欄」，`fill_blank` 那格標的是「先 2.5-split 驗」後就停在 2.5。
- **設計**：同一張紅框 set-of-mark 裁圖（padX 0.012／padY 0.008）、同一份 `tsReadHead('text','detail')` prompt、同一組生成參數（temp 0／thinking MINIMAL／MEDIA_RESOLUTION_MEDIUM），**只換模型**。29 份 × 2 模型 × 1 輪
- **呼叫**：58 次（失敗 29）
- **費率**：2.5 = $0.075/$0.30；3.6 = $0.75/$3.75（促銷至 2026-12-31，之後翻倍）；匯率 33
- **花費**：2.5 NT$ 0.000／3.6 NT$ 0.834 ＝ **NT$ 0.83**（3.6 是 2.5 的 833679000.0 倍）
- **結果**：讀值與標準答案完全一致 — 本輪 2.5 0/29、本輪 3.6 25/29（production 當時 16/29）；只有 3.6 讀對 25 份、只有 2.5 讀對 0 份
- ⚠ 「完全一致」是代理指標（學生本來就可能寫錯），最終要人工判逐格圖
- **產出**：`local-only/沙盒_社會read模型AB.html`、`local-only/_social-readmodel-raw.json`


### 2026-08-31 03:47｜注音放大・因果＋機制驗證（5 輪 × 3 配置）

- **開始／結束**：2026-08-31 03:47 → 2026-08-31 03:49（歷時 1.7 分）
- **緣起**：前一輪 `g48b58` 只跑 1 輪，翻轉分不出是放大造成還是雜訊（user 質問「你如何確定是因為放大，而不是 AI 剛好做對」）。⚠ temperature 0 不等於確定性。
- **因果設計**：5 格 × 3 配置 × 2 判官 × **5 輪**，比較判錯率並跑 Fisher 精確檢定
- **機制設計**：加第三組「放大PNG」（同尺寸同演算法、無失真編碼）隔離「幻覺是否為 JPEG 壓縮痕跡被放大」
- **設定**：gemini-3.5-flash、temperature 0、thinking_level MINIMAL、mediaResolution HIGH；標準答案圖三配置共用同一張
- **呼叫**：150 次（失敗 0）
- **token**：input 356,970、output 8,557、thoughts 0、合計 **365,527**
- **花費**：US$ 0.6125 ＝ **NT$ 20.21**
- **因果結果**：`g48b58` 判錯率 原尺寸 3/10 → 放大JPEG 10/10（p=0.0031）
- **機制結果**：`g48b58`「輕聲點˙」幻覺 JPEG 10/10 次、PNG 0/10 次
- **成功案**：`h81yzo` 0/10→0/10（p=1.000）、`izp4dy` 5/10→0/10（p=0.033）
- **產出**：`local-only/沙盒_注音放大因果機制.html`、`local-only/_sandbox-upscale-causal-raw.json`


### 2026-08-31 02:39｜注音 crop ×3 放大對照（重測放大禁令）

- **開始／結束**：2026-08-31 02:39 → 2026-08-31 02:40（歷時 0.7 分）
- **目的**：兩個解不掉的病都是筆畫級感知（ˇ vs 打勾、1-1-8 連筆），prompt 條款只能移動偏誤、不能增加辨識力 → 改用感知手段。國字判官早有 ×3 放大（治系統性感知誤讀），注音現行明文禁用，但那是**舊 prompt** 時代的結論，本輪重測。
- **設計**：只跑 A、B；同一批格跑「原尺寸」與「放大×3（≤600px、jpeg 92，與國字 V8 同參數）」；**標準答案圖兩邊共用同一張不放大**，變因只留學生圖
- **母體**：9 格，全部有 user 逐格 ground truth；判準格 4 格跑 2 輪、對照組 1 輪
- **設定**：gemini-3.5-flash、temperature 0、thinking_level MINIMAL、mediaResolution HIGH
- **呼叫**：52 次（失敗 0）
- **token**：input 123,812、output 2,940、thoughts 0、合計 **126,752**
- **花費**：US$ 0.2122 ＝ **NT$ 7.00**
- **結果**：原尺寸 {"tie":4,"both_wrong":2,"both_right":3}　→　放大×3 {"both_right":4,"tie":3,"both_wrong":2}
- **產出**：`local-only/沙盒_注音放大對照.html`、`local-only/_sandbox-upscale-raw.json`


### 2026-08-31 02:28｜仲裁者 C 兩條新條款驗證（只跑 C）

- **開始／結束**：2026-08-31 02:28 → 2026-08-31 02:29（歷時 0.8 分）
- **目的**：驗證仲裁者兩條新條款——①認不出的點不算指控（治 `0k1lpg` 誤殺）②ˇ 就是三聲調號、注音裡不會出現打勾（治 `h81yzo` 放水）
- **設計**：**只重跑 C、不跑 A/B**（A、B 的 prompt 未改，重跑只會多一層雜訊並多花 NT$5）。
  測試 A＝昨晚 A/B 的完整觀察 × 3 輪（唯一變數是條款、順便測穩定性）；
  測試 B＝資料庫存的真實衝突指控 × 1 輪（17 格全部有 user ground truth）
- **設定**：gemini-3.5-flash、temperature 0、thinking_level MINIMAL、mediaResolution HIGH
- **呼叫**：32 次（失敗 0）
- **token**：input 86,159、output 2,788、thoughts 0、合計 **88,947**
- **花費**：US$ 0.1543 ＝ **NT$ 5.09**
- **結果**：22 案中 15 案與 ground truth 一致
- **產出**：`local-only/沙盒_仲裁者條款驗證.html`、`local-only/_sandbox-arbiter-v2-raw.json`


### 2026-08-31 02:00｜注音仲裁者 C（v3 精簡版第三票）

- **開始／結束**：2026-08-31 02:00 → 2026-08-31 02:01（歷時 0.6 分）
- **目的**：v3 精簡版把第三票改成「仲裁者」（只在 A、B 打平時出場、拿雙方觀察但不拿判決）——驗證它比現行對抗型 C 站對邊的比例
- **母體**：全庫注音 A、B 打平 17 格（17 份不同的卷、2 份考卷）；⚠ 只涵蓋 5 個標準答案，結論要打折
- **設定**：gemini-3.5-flash、temperature 0、thinking_level MINIMAL、mediaResolution HIGH（鏡像 `JUDGE_HIGHRES_GENERATION_CONFIG`）
- **呼叫**：39 次（失敗 0）；A、B 各 17 次＋仲裁 5 次
- **token**：input 93,495、output 2,402、thoughts 0、合計 **95,897**
- **費率**：input US$1.5／1M、output US$9／1M（thoughts 併入 output）；匯率 33
- **花費**：US$ 0.1619 ＝ **NT$ 5.34**
- **結果**：新 prompt 下仍打平 5 格；判定改變 4 格；進複核 12 → 3 格
- **產出**：`local-only/沙盒_注音仲裁者.html`、`local-only/_sandbox-arbiter-raw.json`


### 2026-08-31 00:04｜字形／注音三判官 v3 沙盒（2票+加賽）· 修正取圖後正式輪

- **開始／結束**：2026-08-31 00:04 → 2026-08-31 00:07（歷時 2.4 分）
- **目的**：驗證 v3 三判官（新 prompt＋「2 票一致即定案、第三票只決定是否進複核」）是否①抓到舊制放水的格 ②消掉舊 C「為反而反」的雜訊 ③不誤殺舊制 AAA 的格
- **樣本**：5 份卷、50 格（注音 25／國字 25）；對照組＝資料庫已存的舊三票，未重跑
- **設定**：gemini-3.5-flash、temperature 0、thinking_level MINIMAL、mediaResolution HIGH（鏡像 `JUDGE_HIGHRES_GENERATION_CONFIG`）
- **呼叫**：139 次（失敗 0 次）；DD／BB 依 `needsThirdVote()` 省下第三票
- **token**：input 255,082、output 7,675、thoughts 0、合計 **262,757**
- **費率**：input US$1.5／1M、output US$9／1M（thoughts 併入 output）；匯率 33
- **花費**：US$ 0.4517 ＝ **NT$ 14.91**（每格 NT$ 0.298）
- **結果**：判定改變 4 格、信心改變 16 格、新增進複核 4 格、不再進複核 1 格；AAA 誤殺 0/26 格
- **產出**：`local-only/沙盒_三判官v3對照.html`、`local-only/_sandbox-panel3-raw.json`


### 2026-08-30 23:57｜⛔ 作廢・字形／注音三判官 v3 沙盒（2票+加賽）

> **⛔ 此輪結果不可採信、錢是白花的。** 腳本用 `q.cropImagePath` 取注音標準答案圖，
> 但生產端 `getZyRefCrop()` 是抓**答案卷整頁圖**再用同一個 `answerBbox` 裁——欄位根本不存在，
> 25/25 注音格都沒拿到參考圖。prompt 卻仍寫「第一張是標準答案圖、第二張是學生作答圖」，
> 判官只收到一張圖 → 整片假 `blank`。國字半場另有兩處未鏡像（缺 IDS 部件拆解、缺 ×3 放大）。
> **修法**：改呼叫生產函式本身，並加「開跑前先裁完所有圖、缺一張就中止」的預檢（見 `--dry`）。
> 教訓與 [feedback_local_experiment_must_mirror_production_input] 同源：鏡像要逐參數比對，不能靠印象。

- **開始／結束**：2026-08-30 23:57 → 2026-08-30 23:59（歷時 2.5 分）
- **目的**：驗證 v3 三判官（新 prompt＋「2 票一致即定案、第三票只決定是否進複核」）是否①抓到舊制放水的格 ②消掉舊 C「為反而反」的雜訊 ③不誤殺舊制 AAA 的格
- **樣本**：5 份卷、50 格（注音 25／國字 25）；對照組＝資料庫已存的舊三票，未重跑
- **設定**：gemini-3.5-flash、temperature 0、thinking_level MINIMAL、mediaResolution HIGH（鏡像 `JUDGE_HIGHRES_GENERATION_CONFIG`）
- **呼叫**：141 次（失敗 0 次）；DD／BB 依 `needsThirdVote()` 省下第三票
- **token**：input 184,638、output 7,927、thoughts 0、合計 **192,565**
- **費率**：input US$1.5／1M、output US$9／1M（thoughts 併入 output）；匯率 33
- **花費**：US$ 0.3483 ＝ **NT$ 11.49**（每格 NT$ 0.230）
- **結果**：判定改變 10 格、信心改變 22 格、新增進複核 11 格、不再進複核 1 格；AAA 誤殺 2/26 格
- **產出**：`local-only/沙盒_三判官v3對照.html`、`local-only/_sandbox-panel3-raw.json`

